{
  "name": "cf-pages-await",
  "version": "1.0.0",
  "description": "Wait for a Cloudflare Pages build to finish",
  "main": "index.js",
  "keywords": [],
  "author": "Daniel Walsh (WalshyDev)",
  "license": "MIT",
  "scripts": {
    "build": "esbuild src/action.ts --platform=node --bundle --outfile=dist/action.js"
  },
  "dependencies": {
    "@actions/core": "^1.6.0",
    "node-fetch": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^16.11.6",
    "esbuild": "^0.13.9",
    "typescript": "^4.4.4"
  }
}
